from setuptools import setup

setup(
    name="strops",
    version="1.0",
    description="A string operations module",  
    author="Nimra",
    py_modules=["strops"], 
    install_requires=[],
)